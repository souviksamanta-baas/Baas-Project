# Project Guidance

- The overall app experience is in Spanish. Keep user-facing UI copy, mock data labels, and review content in Spanish unless a specific integration requires otherwise.
- Use Spanish locale formatting for numbers and currency in the app UI. For example, use `$1.250` instead of `$1,250` for thousands.
